// 그냥 데이터 format을 만들어주는 용도로 사용했다 정도?
// 말 그대로 그냥 Class를 만들어 준 것이다.
class Category {
  constructor(id, title, color) {
    this.id = id;
    this.title = title;
    this.color = color;
  }
}

export default Category;
